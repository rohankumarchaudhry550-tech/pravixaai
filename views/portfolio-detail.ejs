<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%= project.title %> Solution Example | Pravixa AI</title>
  <meta name="description" content="<%= project.summary %>">
  <link rel="canonical" href="https://pravixaai.com/portfolio/<%= slug %>">
    <link rel="icon" href="fa.png" type="image/png">

  <style>
    *,
    *::before,
    *::after {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: Inter, Arial, sans-serif;
      background: #070d19;
      color: #eef7ff;
      overflow-x: hidden;
    }

    /* HEADER */
    header {
      position: sticky;
      top: 0;
      z-index: 1000;
      background: rgba(8,24,39,0.85);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }

    .nav {
      max-width: 1120px;
      margin: auto;
      padding: 16px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 800;
      color: #fff;
      text-decoration: none;
    }

    .brand img {
      height: 36px;
    }

    .links {
      display: flex;
      gap: 20px;
    }

    .links a {
      color: #cde2f5;
      text-decoration: none;
      font-size: 0.95rem;
      position: relative;
    }

    .links a::after {
      content: "";
      position: absolute;
      width: 0%;
      height: 2px;
      bottom: -4px;
      left: 0;
      background: #00c896;
      transition: 0.3s;
    }

    .links a:hover::after {
      width: 100%;
    }

    /* HAMBURGER */
    .menu-toggle {
      display: none;
      width: 28px;
      height: 22px;
      cursor: pointer;
      position: relative;
    }

    .menu-toggle span {
      position: absolute;
      width: 100%;
      height: 3px;
      background: #fff;
      transition: 0.3s;
    }

    .menu-toggle span:nth-child(1){top:0;}
    .menu-toggle span:nth-child(2){top:9px;}
    .menu-toggle span:nth-child(3){top:18px;}

    .menu-toggle.active span:nth-child(1){
      transform: rotate(45deg);
      top:9px;
    }
    .menu-toggle.active span:nth-child(2){
      opacity:0;
    }
    .menu-toggle.active span:nth-child(3){
      transform: rotate(-45deg);
      top:9px;
    }

    /* MOBILE MENU */
    .mobile-menu {
      position: fixed;
      top: 0;
      right: -100%;
      width: min(280px, 82vw);
      height: 100vh;
      background: rgba(8,24,39,0.96);
      backdrop-filter: blur(16px);
      padding: 80px 20px;
      display: flex;
      flex-direction: column;
      gap: 18px;
      transition: 0.35s ease;
      z-index: 1002;
      box-shadow: -18px 0 40px rgba(0,0,0,0.35);
    }

    .mobile-menu.active {
      right: 0;
    }

    .overlay {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.4);
      opacity: 0;
      visibility: hidden;
      transition: 0.3s;
      z-index: 1001;
    }

    .overlay.active {
      opacity: 1;
      visibility: visible;
    }

    /* MAIN */
    main {
      max-width: 1120px;
      margin: auto;
      padding: 70px 20px;
    }

    .eyebrow {
      color: #00c896;
      font-weight: 700;
      font-size: 0.8rem;
      text-transform: uppercase;
    }

    h1 {
      font-size: clamp(2rem,5vw,4rem);
      margin: 10px 0 16px;
    }

    .summary {
      max-width: 800px;
      color: #bdd0e5;
      margin-bottom: 30px;
    }

    /* GRID */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(260px,1fr));
      gap: 18px;
    }

    .panel {
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(0,200,150,0.18);
      border-radius: 16px;
      padding: 20px;
      transition: 0.25s;
    }

    .panel:hover {
      transform: translateY(-4px);
      border-color: #00c896;
    }

    h2 {
      color: #00c896;
      margin-bottom: 8px;
    }

    .tags {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .tag {
      padding: 6px 10px;
      border-radius: 999px;
      background: rgba(0,200,150,0.12);
      font-size: 0.85rem;
    }

    .project-links {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      margin-top: 12px;
    }

    .related-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(220px,1fr));
      gap: 12px;
      margin-top: 12px;
    }

    .related-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      min-height: 58px;
      padding: 14px;
      border-radius: 12px;
      border: 1px solid rgba(0,200,150,0.16);
      background: rgba(255,255,255,0.035);
      color: #eef7ff;
      text-decoration: none;
      font-weight: 700;
      transition: 0.25s;
    }

    .related-card:hover {
      transform: translateY(-3px);
      border-color: #00c896;
      background: rgba(0,200,150,0.08);
    }

    .related-card span {
      overflow-wrap: anywhere;
    }

    .related-card::after {
      content: "View";
      flex: 0 0 auto;
      color: #00c896;
      font-size: 0.82rem;
    }

    /* BUTTONS */
    .actions {
      margin: 30px 0;
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .btn {
      padding: 12px 18px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 700;
    }

    .primary {
      background: linear-gradient(135deg,#00c896,#007bff);
      color: white;
    }

    .secondary {
      border: 1px solid rgba(255,255,255,0.2);
      color: #dbeaff;
    }

    footer {
      text-align: center;
      padding: 20px;
      color: #8fa7bd;
      border-top: 1px solid rgba(255,255,255,0.08);
    }

    /*  MOBILE */
    @media (max-width: 490px) {

      .links {
        display: none;
      }

      .menu-toggle {
        display: block;
      }

      main {
        padding: 42px 16px;
        text-align: left;
      }

      .actions {
        flex-direction: column;
      }

      .btn {
        width: 100%;
        text-align: center;
      }

      .summary {
        margin-bottom: 22px;
      }

      .panel {
        padding: 16px;
      }

      .project-links,
      .tags {
        justify-content: flex-start;
      }

      .related-grid {
        grid-template-columns: 1fr;
      }

      .related-card {
        align-items: flex-start;
        flex-direction: column;
      }
    }
  </style>
</head>

<body>

<header>
  <nav class="nav">
    <a class="brand" href="/">
      <img src="/logo.png">
    </a>

    <div class="links">
      <a href="/">Home</a>
      <a href="/services">Services</a>
      <a href="/portfolio">Portfolio</a>
      <a href="/careers">Careers</a>
      <a href="/contact">Contact</a>
    </div>

    <div class="menu-toggle" onclick="toggleMenu()" id="menuBtn">
      <span></span><span></span><span></span>
    </div>
  </nav>
</header>

<div class="mobile-menu" id="mobileMenu">
  <a href="/">Home</a>
  <a href="/services">Services</a>
  <a href="/portfolio">Portfolio</a>
  <a href="/careers">Careers</a>
  <a href="/contact">Contact</a>
</div>

<div class="overlay" id="overlay" onclick="toggleMenu()"></div>

<main>

  <div class="eyebrow"><%= project.category %> Solution Example</div>
  <h1><%= project.title %></h1>
  <p class="summary"><%= project.summary %></p>

  <div class="grid">
    <section class="panel"><h2>Problem</h2><p><%= project.problem %></p></section>
    <section class="panel"><h2>Solution</h2><p><%= project.solution %></p></section>
    <section class="panel"><h2>Result</h2><p><%= project.result %></p></section>
  </div>

  <section class="panel">
    <h2>Technology Stack</h2>
    <div class="tags">
      <% project.stack.forEach(item => { %>
        <span class="tag"><%= item %></span>
      <% }) %>
    </div>
  </section>

  <% if (project.links && project.links.length) { %>
  <section class="panel">
    <h2>Project Links</h2>
    <div class="project-links">
      <% project.links.forEach(link => { %>
        <a class="btn secondary" href="<%= link.url %>" target="_blank" rel="noopener noreferrer"><%= link.label %></a>
      <% }) %>
    </div>
  </section>
  <% } %>

  <div class="actions">
    <a class="btn primary" href="/contact">Start a Similar Project</a>
    <a class="btn secondary" href="/portfolio">Back to Solutions</a>
  </div>

  <section class="panel">
    <h2>More Solution Examples</h2>
    <div class="related-grid">
      <% relatedProjects.forEach(item => { %>
        <a class="related-card" href="/portfolio/<%= item.slug %>"><span><%= item.title %></span></a>
      <% }) %>
    </div>
  </section>

</main>

<footer>© 2026 Pravixa AI</footer>

<script>
function toggleMenu(){
  document.getElementById("mobileMenu").classList.toggle("active");
  document.getElementById("overlay").classList.toggle("active");
  document.getElementById("menuBtn").classList.toggle("active");
}
</script>

</body>
</html>
